# Credentials and secrets. Never commit any of these.
.cred
*.cred
.env
.env.*
*.key
*.pem
*.crt
*.p12
master.key
secrets/

# Application data directory: databases, TLS material, exports, logs
data/
.expirymanager/
tls/
*.duckdb
*.duckdb.wal
*.duckdb.tmp/
*.sqlite
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.db
*.db-wal
*.db-shm
# Anchored, because an unanchored `exports/` matches every directory of that name anywhere in
# the tree. It matched frontend/src/components/exports, which is why that component is absent
# from the repository: it was written, git ignored it, and nothing ever said so.
/exports/
/logs/
.expirymanager/
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
.coverage.*

# Node
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
